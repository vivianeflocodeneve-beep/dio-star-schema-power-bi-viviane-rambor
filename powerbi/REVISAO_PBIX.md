# Revisão do PBIX

Arquivo final: `StarSchema_Vendas_Viviane_Rambor.pbix`

Verificações realizadas para a entrega:

- arquivo PBIX com estrutura interna íntegra;
- `DataModel` incorporado;
- tela Modelo com as tabelas `tblDCalendario`, `tblDDescontos`, `tblDDetalhes`, `tblDProdutos`, `tblDProdutosDetalhes` e `tblFVendas`;
- `tblFVendas` posicionada no centro e dimensões distribuídas ao redor;
- arquivo final incluído na pasta `powerbi/`.

A página de relatório pode permanecer sem visuais, pois o foco deste desafio é a modelagem dimensional.
